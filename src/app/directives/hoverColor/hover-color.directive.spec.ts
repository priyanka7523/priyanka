import { HoverColorDirective } from './hover-color.directive';

describe('HoverColorDirective', () => {
  it('should create an instance', () => {
    const directive =  [HoverColorDirective];
    expect(directive).toBeTruthy();
  });
});
